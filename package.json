{
  "name": "hitomi-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix indexer && npm run build --prefix streamer && npm run build --prefix downloader",
    "start": "npm run build && systemctl --user start hitomi-indexer hitomi-streamer hitomi-downloader",
    "restart": "npm run build && systemctl --user restart hitomi-indexer hitomi-streamer hitomi-downloader",
    "stop": "systemctl --user stop hitomi-indexer hitomi-streamer hitomi-downloader",
    "logs": "journalctl --user -u hitomi-indexer -u hitomi-streamer -u hitomi-downloader -f"
  }
}
